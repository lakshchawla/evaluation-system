const { system } = require("nodemon/lib/config");

const arr = [];

arr.push("Lakshay");
arr.push("Lakshay");
arr.push("Lakshay");
arr.push("Lakshay");

arr.forEach(element => {
    console.log(element);
});